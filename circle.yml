machine:

  node:
    version: 0.10.22

test:
  override:
    - echo "These are not the tests you are looking for"

deployment:
  production:
    branch: master

    commands:
      - node src/trigger-upstream-job.js